This is a TODO app made using Nodejs as backend and mongoDB as database.

Once the clone is done following steps are required.

1. Install nodejs
2. Install mongodb(Preferably: Make it run as a service)
3. Inside the folder run 'npm install'
4. Open cmd in the folder and run 'node app'